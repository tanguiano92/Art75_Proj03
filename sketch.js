var img;

function preload() {

  img = loadImage("images/butterfly.png");
}

function setup() {
  //createCanvas(windowWidth, windowHeight);
  //background(0)
  //stroke(179, 5, 5);

   //for (let j = 0; j < 10; j ++){
   //for (let i = 0; i < 10; i ++){
  //line (j * 150, 0, windowWidth, i * 150);
  //}

    //for (let j = 0; j < 10; j ++){
   //for (let i = 0; i < 10; i ++){
  //line (j * 150, windowWidth, 0, i * 150);
  //}

       // Notes for loops&Nested loops:
  //for (let i = 0; i < 10; i ++){
  //line (i * 50, 0, windowWidth, i * 50);
 //ine (500, 500, i * 50, 0);

    //for (let i = 0; i < 10; i ++){
  //line (i * 50, windowWidth, 0, i * 50);

    //  for (let i = 0; i < 10; i ++){
  //line (windowWidth, 0, i * 50, i * 50 );

    //    for (let i = 0; i < 10; i ++){
  //line (i * 50, i * 50, 0, windowWidth);

  }
      }





function draw() {
  image(img, 0, 0);

}
}
